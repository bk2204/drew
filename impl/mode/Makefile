PLUGINS_MODE-${CFG_CFB}	+= cfb
PLUGINS_MODE-${CFG_OFB}	+= ofb

MODE_DIR		:= impl/mode
MODE_PLUGINS	:= $(patsubst %,${MODE_DIR}/%,$(PLUGINS_MODE-m))
PLUGINS			+= $(MODE_PLUGINS)

$(MODE_PLUGINS): CPPFLAGS += -I$(MODE_DIR)
